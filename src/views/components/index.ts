import { withInstall } from '@/utils'

import loginPanel from './LoginPanel.vue'
import minePanel from './MinePanel.vue'

export const LoginPanel = withInstall(loginPanel)

export const MinePanel = withInstall(minePanel)
